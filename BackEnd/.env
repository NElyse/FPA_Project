DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=fpa_db
PORT=5000

# JWT secret for signing tokens (if you use JWT elsewhere)
JWT_SECRET=your_super_secret_key_here

# SMTP settings (used to send reset‐password emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=elinsengimana@gmail.com
SMTP_PASS=sdag ikte mlnd gpwv
# Front-end base URL (used to construct the reset link)
CLIENT_URL=http://localhost:5000
CLIENT_URL=http://localhost:3000


